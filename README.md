# Payload
Payload is a mod, that allows players for simple block or item registry. Currently, the mod is <abbr title="Work in Progress">WIP</abbr>.
Here is the current list of all added things:
1. Addons Screen (Work in Progress).
2. `payload` folder in the profile instance with `packs` folder inside.
3. Added block, item and creative mode tabs registry.